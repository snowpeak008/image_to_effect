# VFX Composer

Embedded UPM package for the formal VFX Composer project. The Runtime assembly is player-safe and must never reference `UnityEditor`; Editor tooling depends on Runtime, never the reverse.
