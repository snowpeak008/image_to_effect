# R4 — “火星减少一半”稳定语义局部 Patch

## Raw prompt sent to isolated generator

```text
You are an isolated AI generator for an S2 Patch experiment. Do not inspect the workspace or use tools. Use only this supplied current Recipe, Patch protocol, and task. Return JSON only, no Markdown or commentary.

CURRENT RECIPE:
{"recipeVersion":1,"id":"fireball_2d","name":"Fireball 2D","dimension":"2d","archetype":"projectile","style":"stylized","targetProfile":"mobile_medium","randomSeed":42,"stages":[{"id":"travel","trigger":"on_launch","duration":1.0,"enabled":true,"modules":[{"id":"core","kind":"energy_body","templateId":"T_Core","parameters":{"scale":1.2},"enabled":true},{"id":"embers","kind":"secondary_particles","templateId":"T_Embers","parameters":{"rate":18,"lifetime":0.55},"attachTo":"core","enabled":true}]},{"id":"impact","trigger":"on_hit","duration":0.5,"enabled":true,"modules":[{"id":"burst","kind":"impact_burst","templateId":"T_Burst","parameters":{"count":24,"speed":3.5},"enabled":true}]}],"metadata":{"createdBy":"codex","templateCatalogVersion":"spike-s1"}}

PATCH PROTOCOL: Return a JSON array, not a full Recipe and not an object wrapper. Each array item has exactly op, path, and value. Use op="replace". Every path must use stable semantic IDs: after the /stages segment give a stage id, then after /modules give a module id; never use an array index. Include only requested changes. The target must already exist and the replacement must have the target parameter's valid type and range.

TASK: Reduce the embers by half.
```

The prompt describes only the array protocol and stable-ID rule; it contains no target Patch example and does not state a final path or replacement value.

## First output

```json
[{"op":"replace","path":"/stages/travel/modules/embers/parameters/rate","value":9}]
```

## Validator result for first output

| Check / precise stable path | Observed value(s) | Result |
|---|---|---|
| JSON and Patch shape | Parsed as a bare array of one object; no Recipe object and no `operations` wrapper | pass |
| Operation fields | Exact keys `op,path,value`; `op=replace` | pass |
| Stable semantic target | `/stages/travel/modules/embers/parameters/rate`; uses IDs `travel` and `embers`, no numeric array index | pass |
| Requested semantic and type/range | Current rate `18 / 2 = 9`; value is numeric and lies in `[0,100]` | pass |
| Locality / unknown fields | Exactly one requested replacement; no unrelated operation or field | pass |

## Validator feedback after first output

无；首次输出是合法局部 Patch，未发送错误反馈。

## Correction output

无；无需修正。

**Correction count:** 0  
**Final determination:** 合法稳定语义局部 Patch。
