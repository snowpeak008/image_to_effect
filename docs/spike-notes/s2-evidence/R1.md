# R1 — 标准火球 Recipe（修订契约）

## Raw prompt sent to isolated generator

```text
You are an isolated AI generator for an S2 experiment. Do not inspect the workspace or use tools. Use only the following contract and task. Return JSON only, with no Markdown or commentary.

CONTRACT: A full Recipe has exactly fields recipeVersion=1 (integer), id="fireball_2d", name (string), dimension="2d", archetype="projectile", style="stylized", targetProfile="mobile_medium", randomSeed (integer 0..4294967295), stages (exactly travel then impact), metadata (exactly createdBy and templateCatalogVersion, both strings). Each stage exactly id, trigger, duration, enabled, modules. travel: trigger on_launch, duration number 0.1..10, modules core then embers. impact: trigger on_hit, duration number 0.1..5, module burst. All stages/modules enabled true. Each module has exactly id, kind, templateId, parameters, enabled; only embers additionally has attachTo="core". core uses kind energy_body, templateId T_Core, parameters exactly scale number 0.5..3. embers uses kind secondary_particles, templateId T_Embers, parameters exactly rate number 0..100 and lifetime number 0.1..3. burst uses kind impact_burst, templateId T_Burst, parameters exactly count integer 1..100 and speed number 0.1..10.

TASK: Generate one standard fireball Recipe.
```

## First output

```json
{"recipeVersion":1,"id":"fireball_2d","name":"Standard Fireball","dimension":"2d","archetype":"projectile","style":"stylized","targetProfile":"mobile_medium","randomSeed":1847263950,"stages":[{"id":"travel","trigger":"on_launch","duration":2.5,"enabled":true,"modules":[{"id":"core","kind":"energy_body","templateId":"T_Core","parameters":{"scale":1.2},"enabled":true},{"id":"embers","kind":"secondary_particles","templateId":"T_Embers","parameters":{"rate":35,"lifetime":1.1},"enabled":true,"attachTo":"core"}]},{"id":"impact","trigger":"on_hit","duration":0.8,"enabled":true,"modules":[{"id":"burst","kind":"impact_burst","templateId":"T_Burst","parameters":{"count":24,"speed":4.5},"enabled":true}]}],"metadata":{"createdBy":"s2_ai_generator","templateCatalogVersion":"1.0"}}
```

## Validator result for first output

| Check / precise paths | Observed value(s) | Result |
|---|---|---|
| JSON; root keys exactly `recipeVersion,id,name,dimension,archetype,style,targetProfile,randomSeed,stages,metadata` | Parsed; exact keys; no unknown fields | pass |
| Fixed formal enum paths | `recipeVersion=1`, `id=fireball_2d`, `dimension=2d`, `archetype=projectile`, `style=stylized`, `targetProfile=mobile_medium` | pass |
| Metadata, stages, triggers and topology | Both metadata strings; `travel/on_launch` then `impact/on_hit`; exact stage fields and required module order | pass |
| Module semantic bindings | `core=energy_body/T_Core`; `embers=secondary_particles/T_Embers/attachTo core`; `burst=impact_burst/T_Burst`; exact module fields | pass |
| Value paths | seed `1847263950` integer in range; durations `2.5`,`0.8`; scale `1.2`; rate `35`; lifetime `1.1`; count `24` integer; speed `4.5`, all in declared ranges | pass |
| Unknown fields / out-of-range values / unrelated changes | none / none / not applicable (generation round) | pass |

## Validator feedback after first output

无；首次输出合法，未发送错误反馈。

## Correction output

无；无需修正。

**Correction count:** 0  
**Final determination:** 合法。
