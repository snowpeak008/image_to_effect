# R2 — 更大、更慢、火星更多（修订契约）

## Raw prompt sent to isolated generator

```text
You are an isolated AI generator for an S2 experiment. Do not inspect the workspace or use tools. Use only the following contract, baseline, and task. Return JSON only, with no Markdown or commentary.

CONTRACT: A full Recipe has exactly fields recipeVersion=1 (integer), id="fireball_2d", name (string), dimension="2d", archetype="projectile", style="stylized", targetProfile="mobile_medium", randomSeed (integer 0..4294967295), stages (exactly travel then impact), metadata (exactly createdBy and templateCatalogVersion, both strings). Each stage exactly id, trigger, duration, enabled, modules. travel: trigger on_launch, duration number 0.1..10, modules core then embers. impact: trigger on_hit, duration number 0.1..5, module burst. All stages/modules enabled true. Each module has exactly id, kind, templateId, parameters, enabled; only embers additionally has attachTo="core". core uses kind energy_body, templateId T_Core, parameters exactly scale number 0.5..3. embers uses kind secondary_particles, templateId T_Embers, parameters exactly rate number 0..100 and lifetime number 0.1..3. burst uses kind impact_burst, templateId T_Burst, parameters exactly count integer 1..100 and speed number 0.1..10.

BASELINE semantic values: core scale 1.2; embers rate 18 and lifetime 0.55; burst count 24 and speed 3.5; travel duration 1.0; impact duration 0.5.

TASK: Generate a full fireball Recipe that is bigger, slower, and has more embers than the baseline. Use semantically reasonable in-range values.
```

## First output

```json
{"recipeVersion":1,"id":"fireball_2d","name":"Greater Ember Fireball","dimension":"2d","archetype":"projectile","style":"stylized","targetProfile":"mobile_medium","randomSeed":2849175306,"stages":[{"id":"travel","trigger":"on_launch","duration":1.6,"enabled":true,"modules":[{"id":"core","kind":"energy_body","templateId":"T_Core","parameters":{"scale":1.8},"enabled":true},{"id":"embers","kind":"secondary_particles","templateId":"T_Embers","parameters":{"rate":36,"lifetime":0.8},"enabled":true,"attachTo":"core"}]},{"id":"impact","trigger":"on_hit","duration":0.7,"enabled":true,"modules":[{"id":"burst","kind":"impact_burst","templateId":"T_Burst","parameters":{"count":32,"speed":2.6},"enabled":true}]}],"metadata":{"createdBy":"s2_ai_generator","templateCatalogVersion":"1.0"}}
```

## Validator result for first output

| Check / precise paths | Observed value(s) | Result |
|---|---|---|
| JSON, root fields, formal enums, metadata | Parsed; exact allowed fields; fixed values/types and both metadata strings match contract | pass |
| Stable stage/module IDs and semantic bindings | Required `travel`/`impact`, `on_launch`/`on_hit`, module order, kinds, template IDs, `enabled`, parameter names and sole `attachTo=core` match contract | pass |
| Range checks | seed `2849175306`; durations `1.6`,`0.7`; scale `1.8`; rate `36`; lifetime `0.8`; count `32` integer; speed `2.6`, all typed and in range | pass |
| Requested semantics | `scale 1.8 > 1.2` (bigger); `travel.duration 1.6 > 1.0` (slower); `rate 36 > 18` (more embers) | pass |
| Unknown fields / out-of-range values / unrelated changes | none / none / not applicable (generation round) | pass |

## Validator feedback after first output

无；首次输出合法，未发送错误反馈。

## Correction output

无；无需修正。

**Correction count:** 0  
**Final determination:** 合法。
