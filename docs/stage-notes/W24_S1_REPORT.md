# W24 S1 report — contract and gate framework

Status: source framework implemented; visual validation is intentionally pending under the user's final-batch review instruction.

Delivered:

- Editor-side strict `VfxDesignContract` JSON entry and semantic validation for the ten mandatory segments. It consumes the authoritative lowerCamel JSON directly through Newtonsoft with unknown/duplicate-field rejection; nested `cameraPose`, `resolution`, `bloom`, `budgetCost`, `textureResidency`, and `frameInterval` shapes match the Schema rather than an editor-only approximation.
- The project-local Design Director authority is now the common W24 S1 contract rather than a sustained-only S0 subset. Its lifecycle enum is exactly `sustained | one_shot | event_driven`; reference roles are `composition | colour | material | motion | atmosphere | attachment | lighting | timing`; and anchors remain the formal `world-origin | transform | socket | mesh | bone | screen` set. The Python and C# validators use the same vocabulary.
- Pre-C0 capture binding is explicit: `PENDING_FIRST_FORMAL_BUILD` requires `pending:formal-build` for scene/build identities and a real capture-tool hash. All-zero hashes are rejected. The four pending Contract/Trace files are immutable bootstrap receipts. Only the internal S5 transaction can write their first Manifest binding; `W24CandidateIdentityFreezer` then writes a separate `docs/vfx-candidates/<effectId>/C0/` Contract/Trace/receipt package with real scene/build/GUID identities and status `C0_CAPTURE_PENDING`.
- Canonical contract identity follows the same rule as the compatibility-named `validate_s0_contract.py`: UTF-8 JSON, recursively sorted object keys, compact separators, Unicode preserved, and root `contractHash` omitted. All four formal contracts (`sustained_flame_3d` plus the three S3 baselines) are direct regression inputs and pass the same validator.
- Reciprocal `VfxImplementationTrace` model and validator that binds each requirement to concrete Unity identities and immutable evidence, rejects missing/extra trace coverage, and enforces authority/cross-evidence separation. `VfxImplementationTraceJson` is the strict lowerCamel entry used by production integration; it rejects duplicate/unknown members rather than relying on ad-hoc deserialization.
- L0–L4 permission policy: only an explicit user binding can create L4.
- The ordinary exact-plan production gate rejects both bootstrap and C0-pending traces; a later write-once `FORMAL_EVIDENCE_BOUND` Trace is mandatory after real capture.
- Small/Standard/Hero templates; visual carrier matrix; semantic pattern library; error substitution catalog; machine and human report templates.
- Frozen known-cheat checks for whole-image fake fragments, static fake trails and additive fake light. A cheat only clears when its authority fails and an independent cross-evidence record alarms.

Regression coverage additionally rejects unknown contract fields, missing nested capture resolution, a non-object `layer.budgetCost`, invalid pre-C0 identities, and lifecycle/role/anchor drift. Completion/interrupt exits and layer continuity entries remain semantic descriptions as permitted by the Schema; the validator does not incorrectly require them to equal state/layer IDs.

Current isolated verification: `W24S1ContractAndTraceTests` passed `15/15` against the current
four contracts and trace model, with `0 failed / 0 skipped / 0 inconclusive`; the isolated Unity
process exited normally with code `0`. The primary durable evidence index is
`D:/WorkWork/Assist/image_to_smart/artifacts/vfx-evidence/w24-stage-focused/run-20260825T143716Z/receipt.json`
(`sha256:de258073b98e2cec9642eb4400543cee0f16231463a8a4a737c7424ac5c74fcc`); its
`9 gates / 21 files` hash-and-count revalidation reports `0` issues. The byte-bound XML is
`D:/WorkWork/Assist/image_to_smart/artifacts/vfx-evidence/w24-stage-focused/run-20260825T143716Z/s1-contract-trace.xml`
(`sha256:a334ab7872f9810fb40881fec5a60e19d42aae212a7c671cf2b20a3f687df2f2`).
This run did not touch the user's canonical Editor. It proves the focused contract/trace machine
gate only; it does not produce a visual verdict, L3, L4, or commercial authority.

Not claimed:

- No visual QA verdict, L3, or L4 was generated by this framework.
- S0b machine evidence is now sealed, but it does not grant visual authority; S1’s final production lock remains contingent on the still-pending cross-stage authority and final human review.
