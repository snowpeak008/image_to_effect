# Cohort K preregistered Patch-only requirements

## K1

Return a Patch only that changes canonical impact module `burst` speed to the legal value `4.4`.

## K2

Return a Patch only that disables canonical launch module `launchFlash` without removing it.

## K3

Return a Patch only that adds canonical travel module `sparkle_embers`: `secondary_particles`, template `PFT_2D_Embers`, attached to `core`, with rate `12`, lifetime `0.65`, and enabled `true`.
