# Cohort K evidence — Patch-only, complete 3/3 pending independent review

K is a new, independent Patch-only recovery batch with exactly `K1`–`K3` and an exact 3/3 acceptance threshold. It does not alter Cohort I or Cohort J: I Recipe remains 4/5 and J remains the historical terminal 2/3 failure.

Before dispatch, the K freeze derives each compact payload from the formal canonical Recipe and the live Catalog. Each payload has only these sections: `TASK`, `PATCH_OPERATION_SYNTAX`, `PATH_RULES`, `RECIPE_CONTEXT`, optional `CATALOG_FACTS`, and `OUTPUT=bare array`. `PATH_RULES` fixes the stable ID-based replace/disable/add paths, requires complete add-module values with matching IDs, and forbids array indexes, wrappers, and Markdown. It contains no canonical Patch example, complete Recipe, full schema, parameter table, or unrelated stage/module. The evidence and one-file-per-key temp copies are byte-identical and hash-bound by short envelopes.

The isolated `gpt-5.6-terra` / high / `fork_turns:none` author made one payload-read command and returned a bare Patch array. Initial outputs were recorded with the frozen envelope witness and real isolated `VfxPatchService` report: K1 and K2 succeeded at revision/history `1->2` and are final; K3 initially failed at revision `1->0` because `attachedTo` is an unknown Recipe field. Main Agent then sent the frozen K3 repair1 envelope in the same thread. Its corrected output succeeded at revision/history `1->2`, byte-matches K3 final, and no repair2 was prepared. The non-Explicit K final gate passed exact 3/3 with real isolated applies/history/effects; the formal M6 gate also passed. The transport records disclose that the host cannot provide wire payload or child-tool-trace readback.

If a failed K attempt needs repair, the frozen repair payload contains the complete prior report and the authoritative complete acceptance operation generated from the frozen spec. Repairs stay in the initial thread, are limited to attempts 1 and 2, and are refused after success.
