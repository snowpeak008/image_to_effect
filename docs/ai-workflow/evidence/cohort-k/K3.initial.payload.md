TASK
Return a Patch only that adds canonical travel module `sparkle_embers`: `secondary_particles`, template `PFT_2D_Embers`, attached to `core`, with rate `12`, lifetime `0.65`, and enabled `true`.

PATCH_OPERATION_SYNTAX
Return one JSON array with exactly one object. Allowed forms: replace {op,path,value}; disable {op,path}; add {op,path,value}.

PATH_RULES
replace /stages/{stageId}/modules/{moduleId}/parameters/{parameter}; disable /stages/{stageId}/modules/{moduleId}; add /stages/{stageId}/modules/{newModuleId}, value is a complete module object and value.id equals newModuleId. Never use array indexes, wrappers, Markdown, prose, or fences.

RECIPE_CONTEXT
{"stage":{"id":"travel","trigger":"after_previous"},"modules":[{"id":"core","kind":"energy_body","templateId":"PFT_2D_FireCore","parameters":{"scale":1.2},"enabled":true}]}

CATALOG_FACTS
{"templateId":"PFT_2D_Embers","parameters":{"rate":{"type":"float","min":4,"max":36},"lifetime":{"type":"float","min":0.25,"max":1.1}}}

OUTPUT=bare array
