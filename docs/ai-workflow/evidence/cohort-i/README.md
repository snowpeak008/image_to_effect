# Cohort I preregistration — awaiting dispatch approval

I is the only proposed countable replacement for the invalid/noncountable H cohort. It has exactly `I1`–`I5` Recipe questions (at least 4/5 must succeed) and `P1`–`P3` Patch questions. A–H remain historical evidence and are not re-scored.

Each isolated `gpt-5.6-terra` / `high` / `fork_turns:none` author receives only a short, file-backed envelope. The envelope permits one `exec_command` invocation to read one specified absolute payload file in `C:\Users\admin\AppData\Local\Temp\vfxcomposer-s9-cohort-i`, verify its stated SHA-256, and then return raw Recipe/Patch JSON. It forbids every other tool, file, directory, workspace, and network access. The exact full payload is written at freeze time both in that explicit temp directory and as a byte-identical workspace evidence copy; both hashes are recorded.

On failure the same agent/thread gets a short repair envelope through `followup_task`, never a new agent. Before that call, the full repair payload (fixed instruction plus complete preceding machine report after LF-only normalization) is written once to temp and workspace evidence, hashes and envelope are written, and work pauses for the primary Agent to inspect the target/name/path/hash and personally send the envelope. The host exposes no wire-payload or child-tool-trace readback; local exact copies, hashes, and the primary Agent's witnessed tool call are not an end-to-end machine proof and this limitation is disclosed in all transport records.

Before explicit approval no I output, attempt, report, final, repair, transport witness, Recipe asset, history, or Generated asset may exist.
