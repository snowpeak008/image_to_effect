# Cohort I preregistered natural-language prompts

## I1

Create a complete buildable 2D projectile Recipe with ID `i1_river_comet` for `pc_editor`: keep the core at its standard scale, use a long broad trail and no ember module, then finish with a gentle burst and a small shockwave ring.

## I2

Create a complete buildable 2D projectile Recipe with ID `i2_glass_spark` for `mobile_medium`: make a small bare core with no trail, sparse long-lived embers, a forceful lower-speed burst, and a large shockwave ring.

## I3

Create a complete buildable 2D projectile Recipe with ID `i3_brazier_bead` for `pc_editor`: use a large core, a narrow short trail, dense short-lived embers, a contained impact burst, and a small shockwave ring.

## I4

Create a complete buildable 2D projectile Recipe with ID `i4_rail_flare` for `mobile_medium`: use standard core scale, a long narrow trail and no embers, then a strong faster burst with a standard-sized shockwave ring.

## I5

Create a complete buildable 2D projectile Recipe with ID `i5_aurora_seed` for `pc_editor`: use a small core, a broad short trail, dense long-lived embers, a gentle lower-speed burst, and a large shockwave ring.

## P1

Return a Patch only that changes the stable canonical travel module `trail` width to the legal value `0.30`.

## P2

Return a Patch only that disables the stable canonical travel module `trail` without removing it.

## P3

Return a Patch only that adds a second legal ember module named `afterglow_embers` to canonical travel, attached to the stable canonical `core` module.
