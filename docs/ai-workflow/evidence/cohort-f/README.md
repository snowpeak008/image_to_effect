# Cohort F

This is the final countable S9 cohort. Its inputs, exact natural-language prompts, semantic acceptance specification, and repair protocol were preregistered before any F agent was dispatched. `contract-snapshot.md` is one-time exported and hash-verified; never overwrite it. A Recipe round is successful only when JSON parsing, real `VfxCompiler.Build`, and the preregistered semantic assertion all succeed. The aggregate threshold is at least four of five. A Patch round is successful only when actual Apply, revision/history, and its preregistered effect assertion succeed.
