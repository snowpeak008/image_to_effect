# Cohort J preregistered Patch-only requirements

## J1

Return a Patch only that changes canonical launch module `launchFlash` size to the legal value `1.4`.

## J2

Return a Patch only that disables canonical impact module `shockwave` without removing it.

## J3

Return a Patch only that adds canonical travel module `linger_embers`: `secondary_particles`, template `PFT_2D_Embers`, attached to `core`, with rate `8`, lifetime `0.9`, and enabled `true`.
