# Cohort E — not yet dispatched

E is the only candidate final cohort. Its formal source bundle is [contract-snapshot.md](contract-snapshot.md) (SHA-256 recorded in its header). Each exact Agent wrapper is the immutable snapshot concatenated with its own frozen suffix in [prompts.md](prompts.md); it does not refer to D. D1/D2 remain diagnostic only and cannot be counted. The pre-existing evidence test hard-requires exactly `E1`–`E5` final Recipe files; it intentionally fails before dispatch/completion.
