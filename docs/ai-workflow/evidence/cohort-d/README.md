# Cohort D preregistration (required before dispatch)

This is the next countable cohort. Exact frozen prompts are [prompts.md](prompts.md); machine-readable, pre-output acceptance is [acceptance-spec.json](acceptance-spec.json). Each Recipe success requires real Build plus its semantic assertions; each Patch success requires Apply/Build, expected revision change, and its operation effect. Agents see only formal docs and the exact natural prompt, not the acceptance spec.
