# Retired — do not dispatch

The v2 freeze is retained only for audit. Its `broader`, `denser`, and `stronger` prompts were not all constrained as values greater than their defaults. No AI agent was dispatched from it. Use a later versioned freeze with executable greater-than-default acceptance rules.
